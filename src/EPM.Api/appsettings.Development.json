{
  "//": "Development overrides. Deliberately carries NO connection string and NO passwords — this file is committed, and a credential in it is a credential in source control. Supply them with `dotnet user-secrets` (see README > Configuration), or run the whole stack with `docker compose up`, which injects them from your .env.",

  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Information",
      "Microsoft.EntityFrameworkCore.Database.Command": "Information"
    }
  }
}
